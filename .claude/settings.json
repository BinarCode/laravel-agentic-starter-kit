{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/eduardlupacescu/Sites/work/restock && composer test 2>&1; EXIT_CODE=$?; if [ $EXIT_CODE -ne 0 ]; then echo '❌ Tests failed. Fix issues before committing.'; exit 1; fi"
          }
        ]
      }
    ]
  }
}
